- args:
    sql: CREATE TABLE "users"("user_id" bigserial NOT NULL, "name" text NOT NULL,
      "user_name" text NOT NULL, "email_id" text NOT NULL, "mobile_no" bigint NOT
      NULL, "age" integer NOT NULL, PRIMARY KEY ("user_id") )
  type: run_sql
- args:
    name: users
  type: add_existing_table_or_view
